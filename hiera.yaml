---
version: 5
defaults:
  datadir: hierdata
  data_hash: yaml_data
:hierarchy:
  - name: "Node data"
    path: "nodes/%{::trusted.certname}"
  - name: "FQDN data"
    path: "nodes/%{::fqdn}"
  - name: "Common"
    path: "common.yaml"
